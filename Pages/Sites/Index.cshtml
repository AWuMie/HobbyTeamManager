@page
@model MySqlTestRazor.Pages.Sites.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Site[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Site[0].Logo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Site[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Site[0].Rules)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Site[0].ConfirmationMode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Site[0].TeamColor1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Site[0].TeamColor2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Site) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Logo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rules)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConfirmationMode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TeamColor1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TeamColor2)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
